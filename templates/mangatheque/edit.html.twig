{% extends 'base.html.twig' %}

{% block title %}Edit Mangatheque
{% endblock %}

{% block main %}
	<h1 class="text-center my-4 fw-bold" style="background: linear-gradient(90deg, #FF4500, #FFA500, #FFD700);
			           -webkit-background-clip: text;
			           color: transparent;
			           font-family: 'Arial', sans-serif;
			           text-transform: uppercase;">
		<i class="bi bi-pencil-fill"></i>
		Edit Mangatheque
	</h1>


	{{ include('mangatheque/_form.html.twig', {'button_label': 'Update'}) }}

	<br>
	<div class="text-center">
		<a class="btn mt-auto btn-sm btn-secondary" href="{{ path('app_mangatheque_index') }}">back to list</a>
	</div>

{% endblock %}
